{
  "name": "reviseliketeacher",
  "version": "1.0.0",
  "description": "NEET PG preparation platform with personalized revision planning",
  "main": "api/server.js",
  "scripts": {
    "start": "node api/server.js",
    "dev": "nodemon api/server.js",
    "db:migrate": "psql -d reviseliketeacher -f database/schema.sql",
    "db:seed": "psql -d reviseliketeacher -f database/seed_data.sql",
    "db:reset": "psql -d reviseliketeacher -c 'DROP SCHEMA public CASCADE; CREATE SCHEMA public;' && npm run db:migrate && npm run db:seed"
  },
  "keywords": [
    "education",
    "neet-pg",
    "medical-exams",
    "revision-scheduler"
  ],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "api",
    "frontend"
  ]
}

